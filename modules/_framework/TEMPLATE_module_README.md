<!-- 模块 README 模板:复制本文件为模块目录下 README.md 并逐段填写。图中文字英文,正文中文。 -->

# NNN · <模块中文名> <英文名>

> 一句话定位:这个模块**输入什么→做什么→出什么图**。

| | |
|---|---|
| **语言 / 主依赖** | R 或 Python · `pkg1` `pkg2` |
| **一句话用途** | … |
| **输入** | `example_data/xxx.csv` |
| **输出** | `results/`(运行生成) · 展示图见 `assets/` |

---

## ① 输入数据

**文件**:`<name>.csv`(类型:csv/tsv/h5ad/rds/maf;orientation:行=基因/样本)

| 列名 | 类型 | 必需 | 示例 | 说明 |
|------|------|:---:|------|------|
| `Gene` | str | ✔ | `TP53` | … |
| … | | | | |

**命名/格式约定**:(如样本名须以 `_con`/`_tra` 结尾等,无则写"无")

**样例(前 3 行)**:
```
Gene,...
TP53,...
```

## ② 方法 / 原理

简述算法与流程(关键步骤 1-2-3)+ 核心方法引用(包/论文)。

## ③ 用途

回答什么科学问题;典型应用场景。

## ④ 特点 / 亮点

- turnkey:一条命令即跑;
- 顶刊级合成图:…
- …

## ⑤ 输出结果图

| 文件 | 图型 | 说明 |
|------|------|------|
| `assets/xxx.png` | … | … |

![主图](assets/xxx.png)

---

## 运行

```bash
# 零改动跑示例
Rscript NNN_模块.R
# 换成自己的数据
Rscript NNN_模块.R --input data/你的.csv --outdir results/run1
```

## 依赖安装

```r
install.packages(c("pkg1","pkg2"))
# 或 BiocManager::install(c("..."))
```
